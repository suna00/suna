# 운영 API 캐시 서버 (10.75.6.144) 설정
# - 운영 API 캐시는 API 게이트웨이(48) 로 부터 API 요청을 받는다
# - 운영 API 캐시는 CMSWEBWAS(130) 와 통신할 수 있음(필수. API)
# - 운영 API 캐시는 RDS 와 통신하지 않는다 - datasource.json 에 설정이 필요없음


spring:
  profiles: aws-apicache
  http:
    multipart:
      max-request-size: 10MB
      max-file-size: 20MB

server:
  port: 8080

jwtConfig:
  issuer: I-ON
  secretKey: dkdldhs!@#$
  headerPrefix: SDP
  headString: X-Authorization
  tokenExpirationTime: 15 #분
  refreshTokenExpTime: 60 #분


# 스케일업할 때 members 가 자동으로 늘어나나?
cluster:
  mode: all
  groups: all
  members:
    - 10.75.6.144
    - 127.0.0.1
    - localhost

infinispan:
  cachePath: /home/ion/api/resource/ice2/cache

tomcat:
  access-log-enabled: true
  access-log-pattern: "%h %l %u %t \"%r\" %s %b %D"
  basedir: tomcat

logging:
  config:
    classpath:
      - logback-access-dev-2.xml
      - logback-dev-2.xml

file:
  default:
    path: /home/ion/api/resource/ice2/files


i18n:
  defaults: eng

cjapi:
  pip:
    programurl: http://pipapi.itvs.cjenm.com/json/data/v1.0/global/programinfo.asp
    clipmediaurl: http://pipapi.itvs.cjenm.com/json/data/v1.0/global/clipmediainfo.asp

aws:
  s3:
    bucketName: cjmwave-prd
    bucketKey: ion
    accessKey: AKIAJWGJBWJU3TLXANWA
    secretKey: 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



temp-file:
  dir: /home/ion/api/resource/ice2/cache