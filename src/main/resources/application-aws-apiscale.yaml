# 운영 API 게이트웨이 서버 (10.75.6.48) 설정
# - 운영 API GW 는 CMSWebWAS(130) 와 통신할 수 있음(필수. 사용자 데이터 저장)
# - 운영 API GW 는 API CACHE(144) 와 통신할 수 있음(필수. API)
# - 운영 API GW 는 CMS RDS 와 통신할 수 있음(선택. 성능) - datasource.json
# - 운영 API GW 는 회원 RDS 와 통신할 수 있음(필수. 투표) - datasource.json


spring:
  profiles: aws-apiscale
  http:
    multipart:
      max-request-size: 10MB
      max-file-size: 20MB

server:
  port: 8080

jwtConfig:
  issuer: I-ON
  secretKey: dkdldhs!@#$
  tokenPrefix: SDP
  headString: X-Authorization
  tokenExpirationTime: 15 #분
  refreshTokenExpTime: 60 #분


# 스케일업할 때 members 가 자동으로 늘어나나?
cluster:
  mode: all
  groups: all
  members:
    - 10.75.6.165
    - 127.0.0.1
    - localhost

infinispan:
  cachePath: /data/ion/api/resource/ice2/cache

tomcat:
  access-log-enabled: true
  access-log-pattern: "%h %l %u %t \"%r\" %s %b %D"
  basedir: tomcat

logging:
  config:
    classpath:
      - logback-access-dev-2.xml
      - logback-dev-2.xml

file:
  default:
    path: /data/ion/api/resource/ice2/files


i18n:
  defaults: eng

cjapi:
  pip:
    programurl: http://pipapi.itvs.cjenm.com/json/data/v1.0/global/programinfo.asp
    clipmediaurl: http://pipapi.itvs.cjenm.com/json/data/v1.0/global/clipmediainfo.asp



temp-file:
  dir: /data/ion/api/resource/ice2/cache

image:
  prefixUrl: http://g-api-elb-579243851.ap-northeast-2.elb.amazonaws.com/image/

mobile:
  push:
    fcmApiKey: AAAA-z1uhx4:APA91bHlHSqGakLe7RKY1fuvTpUyx1hZKX5BG7ydfK96Kfy9bCX0upciPQ6UlpOXECEcc4tsYFiiEUy6aHSWWMZwwN-V6rtu-aeOAGg0_r6GAFaVkJF3jEOxiO4kblsi4tNDOAApMZx2
    fcmApiUrl: https://fcm.googleapis.com/fcm/send